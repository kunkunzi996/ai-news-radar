name: Update AI News Snapshot

on:
  push:
    branches:
      - master
    paths-ignore:
      - "data/**"
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

concurrency:
  group: update-ai-news-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  update:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: "requirements.txt"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Update data
        env:
          RADAR_SOURCE_SCOPE: tested_creator_sources
          # Deployed source scope is intentionally limited to the three tested
          # creator sources: Bilibili, local MediaCrawler Douyin, and local
          # MediaCrawler Xiaohongshu.
          BILIBILI_DYNAMIC_ENABLED: ${{ vars.BILIBILI_DYNAMIC_ENABLED || '1' }}
          BILIBILI_DYNAMIC_UIDS: ${{ vars.BILIBILI_DYNAMIC_UIDS || vars.BILIBILI_DYNAMIC_UID || '505301413,316183842,3546884870244925,50247550,385670211,313819014' }}
          BILIBILI_DYNAMIC_SOURCE_NAMES: ${{ vars.BILIBILI_DYNAMIC_SOURCE_NAMES || vars.BILIBILI_DYNAMIC_SOURCE_NAME || 'Koji杨远骋at十字路口,技术爬爬虾,杰森的效率工坊,清华姜学长,秋芝2046,anno大笨蛋' }}
          BILIBILI_DYNAMIC_MAX_ITEMS: ${{ vars.BILIBILI_DYNAMIC_MAX_ITEMS || 10 }}
          BILIBILI_DYNAMIC_MAX_PAGES: ${{ vars.BILIBILI_DYNAMIC_MAX_PAGES || 8 }}
          BILIBILI_COOKIE: ${{ secrets.BILIBILI_COOKIE }}
          MEDIACRAWLER_DOUYIN_ENABLED: ${{ vars.MEDIACRAWLER_DOUYIN_ENABLED }}
          MEDIACRAWLER_DOUYIN_JSONL: ${{ vars.MEDIACRAWLER_DOUYIN_JSONL }}
          MEDIACRAWLER_DOUYIN_SOURCE_NAME: ${{ vars.MEDIACRAWLER_DOUYIN_SOURCE_NAME }}
          MEDIACRAWLER_XHS_ENABLED: ${{ vars.MEDIACRAWLER_XHS_ENABLED }}
          MEDIACRAWLER_XHS_JSONL: ${{ vars.MEDIACRAWLER_XHS_JSONL }}
          MEDIACRAWLER_XHS_SOURCE_NAME: ${{ vars.MEDIACRAWLER_XHS_SOURCE_NAME }}
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          python scripts/update_news.py --output-dir data --window-hours 24 --archive-days 21

      - name: Commit and push changes
        env:
          EMAIL_DIGEST_PUBLISH: ${{ secrets.EMAIL_DIGEST_PUBLISH || vars.EMAIL_DIGEST_PUBLISH }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # Stage the whole data dir so newly added output files can never be
          # silently dropped again (the daily-brief/stories-merged freeze was
          # caused by a stale filename whitelist here). .gitignore keeps
          # data/email-digest.json out of this add.
          git add data/
          if [ -f data/email-digest.json ] && [ "$EMAIL_DIGEST_PUBLISH" = "1" ]; then
            git add -f data/email-digest.json
          fi
          # Self-check: fail loudly if anything under data/ is still unstaged,
          # instead of green-but-stale runs.
          if [ -n "$(git status --porcelain --untracked-files=all -- data/ | grep -v '^[MADRC]' || true)" ]; then
            echo "::error::Generated files under data/ were not staged:"
            git status --porcelain --untracked-files=all -- data/ | grep -v '^[MADRC]'
            exit 1
          fi
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "chore: update ai news snapshot"
          git push
