# PROJECT_STATE

## 下一轮入口（2026-07-18 更新）

- **主工作区同步与洁癖（2026-07-18）**：主工作区已从 `853e6f4` 快进到
  `1757d34`，与 `origin/master` 一致。本机旧的 7 月 14 日数据快照、临时截图、实测脚本与
  历史计划文件已整体保护到 `stash@{0}`（`0e94dbf`），不应整批恢复或提交；原有 6 条
  GitHub Release 历史备份仍在 `stash@{1}`（`a8d0acd`），严禁丢弃。下一轮开始先检查
  `git status --short --branch` 与 `git stash list`，恢复内容必须逐个按用途评估。

- **2026-07-18 工作台收藏桥（雷达侧）已合入并推送 `master`**：功能提交为
  `4badc1b 功能：接入工作台收藏桥`，隔离 worktree 为
  `E:\AI-news-reader\ai-news-radar-workbench-bridge`，分支为
  `feature/workbench-collect-bridge`，基线来自 `origin/master` 的 `39375d3`；原工作区的运行数据、
  未跟踪文件和 `stash@{0}` 均未触碰。已实现 iframe 白名单握手、收藏回执配对、卡片收藏按钮及 4 组真实
  iframe E2E；新增桥接 E2E 为 `4 passed`，完整 `npm run test:e2e` 为 `24 passed`，两份 Node 语法检查与
  `git diff --check` 均通过。测试期间临时停止的 `万有 OMNIA 服务` 已恢复运行，8765 端口已由工作台重新监听。
  用户已在 `万有 OMNIA → AI 雷达 → 本机版` 确认卡片显示收藏按钮并通过人工验收；独立打开雷达不显示该按钮是预期行为。
  本轮未改 Python、`.github/workflows/**`、`config/**` 或 `data/**`。下一轮只需在 GitHub Pages 部署完成后，从工作台
  “公网版”确认收藏按钮出现并完成一次真实收藏；若 Pages 未更新，先检查本次推送后的工作流状态。

- **2026-07-16 微信公众号退订历史清理修复已完成线上灰度与真实验收**：代码在独立 worktree
  `E:\AI-news-reader\ai-news-radar-wechat-cleanup`、分支 `fix/wechat-unsubscribe-cleanup` 施工，功能提交
  `fdec276`，合并最新主线后普通推送至 `master`，全程未 force/rebase 覆盖远端。原脏 master、用户
  stash 和本机 `data/**` 未改；当前远端运行模式为 `on`，workflow 已恢复 `active`。
  - **最终业务契约**：sidecar 数据库一次读取后生成 schema 2 权威快照；`known` 是仍存在的非系统
    Feed，`active` 与 `DB.get_all_mps()` 同口径。Feed hard delete 才清对应稳定 `feed_id` 的历史；
    `status=0` 只停采，因 ID 仍在 `known` 中而保留历史。exporter 只消费该权威输入，不再把
    `/rss/fresh` 或 `WE_MP_RSS_FEEDS` 当有效订阅名单。
  - **完整安全门**：manifest、JSONL、订阅快照必须在同一 bridge commit，并通过 schema 2、路径边界、
    哈希、条数、集合和通道状态校验。缺失或非法 `feed_id` 的行在 `RawItem` 前拒绝；无 ID、快照不完整、
    commit 不符、通道失败或任何门控不通过都 fail-safe，一条不删。清理只按 ID，禁止按名称、URL 或
    本轮文章集合推断；回滚只按 `item_id` 精确回插，禁止整份 archive 覆盖。
  - **真实 bridge 与迁移**：bridge commit `2295f32` 发布 schema 2 三件套，60 条文章、known/active=3/3，
    完整哈希链通过。维护窗口基线 `00daa9f` 的 archive 共 625 条、微信 103 条；当时缺 ID 的 43 条全部
    唯一补齐，0 未匹配、0 冲突、0 删除、覆盖率 100%。迁移提交 `b9315ba`，完整 Radar 重建后总数和
    所有通道数量均未下降。
  - **真实灰度结果**：`off` run `29484708544` 确认 103/103 微信记录有 ID、候选/删除 0；`audit` run
    `29484779366` 的 30 个唯一候选只属于 `MP_WXS_3893127105 / 财联社`，异常候选 0；`on` run
    `29484879533` 精确删除这 30 条，archive 625→595、微信 103→73、其它通道下降 0；下一轮 run
    `29484975158` 候选/删除均为 0，财联社未反弹。清理提交为 `399fb67`，清理前回滚基线为 `1f89713`。
  - **自动与浏览器验收**：`test_we_mp_rss_sync_once.py` 17 passed；`test_we_mp_rss_jsonl_source.py`
    22 passed；`test_orphan_subscription_cleanup.py` 20 passed（另 5 subtests）；合并最新主线后全量 pytest
    529 passed、1 warning（另 98 subtests）；PowerShell AST 解析错误 0，`git diff --check` 退出码 0。
    公网页微信公众号栏目显示 73 条、4/4 源正常；搜索财联社为 0，猫笔刀/卡尔的AI沃茨/数字生命卡兹克
    分别有 25/23/25 条可见记录，控制台错误 0。新 worktree 没有独立
    `.venv`，验收从该 worktree cwd 使用原仓库只读虚拟环境
    `E:\AI-news-reader\ai-news-radar-run\.venv\Scripts\python.exe` 执行。
  - **临时 bridge 验收**：文章与快照都不变时不提交；仅快照语义改变时提交；只改 `generated_at`
    不制造提交；`-SkipSync` 不改 HEAD 或正式三件套；三件套同 commit 且全哈希通过；坏 `feed_id`
    行不入 archive；`status=0` 停采留历史；hard delete 只命中目标 ID；精确恢复后新增数据仍保留。
  - **精确回滚**：紧急时把 mode 保持/切回 `off`；代码用正常 revert，不强推、不重写历史；数据恢复
    从 `1f89713` 取本次 30 个目标 `item_id`，用 `scripts/restore_we_mp_cleanup.py` 只补当前 archive 缺失项，
    前后核对 SHA256、总条数和 site_id/source 分布，再重建派生文件。
  - 方案与施工边界：`计划/微信公众号退订后历史消息残留-修复方案.md`。后续 hard delete 会由 `on`
    模式按同一 ID-only 契约清理；`status=0` 仍只停采、不删历史。任何异常先把 Actions 变量切回 `off`。
## GitHub 星标安全同步 V3：真实上线完成（2026-07-16）

- 功能与后续重复同步修复均已合并到 `master`；真实验收完成时的功能与数据基线为 `47286b35ccef76819df37d7b394936cf00ce1422`，当时 `HEAD=origin/master`、ahead/behind 为 `0/0`。
- 真实账号 `kunkunzi996`（数字 account id `284580915`）已绑定；当前公开星标 15 个，对应 15 个受管信源。固定门禁仍为单账号、最多 50 个公开星标、第 51 个整次中止、每仓库每 UTC 日最多一个最新 commit 快照。
- §17.2 真实验收已完成：临时取消并恢复 `joeseesun/qiaomu-goal-meta-skill` 星标，停用 operation commit 为 `450d6b5f42fabc11bbd39c4a497f9871d132ccf7`，恢复为 `b8e51e7fa5330ff14d430955145879ed42110e8e`；随后解绑为 `c6643d99e1bb89fece21d74230419891c1cd26de`，重新绑定为 `cb21d9bcb16de63718bc7f0e5f7c026a0ffbaca1`。四个提交都只修改 `config/online-sources.json`，并带有独立 operation trailer。
- 取消星标只自动停用、不删除信源、不触发 pending purge；目标 source id `online_github_repo_1266385233`、repo id `1266385233` 在停用/恢复与解绑/重绑全程稳定，历史仍保留。此前取消星标的 `AlkaidLab/moonlight-harmony` 保持停用且历史仍为 1 条。
- 重新绑定后再次 Preview/Apply 得到 `no_change`：HEAD、配置字节、OPML、`updated_at=2026-07-16T13:26:41Z` 均未变化。最终配置 SHA256 为 `C4B1E08F8D6F2CF61E5986B8BACD5D6F188778FB8984A35172E7111808CF88E8`，OPML SHA256 为 `25A7984823CA46F4591CEC90E23A7707455BE596A882960EF49E0BE962B67058`。
- 公网 Actions 与 Pages 已成功：Update run `29502225101`、Pages run `29502283288`；公网配置/OPML 哈希与本地一致，GitHub 采集状态为 eligible/succeeded 15、failed/deferred 0、fallback 5。
- 自动验收：专项 pytest `344 passed, 91 subtests`；全量 pytest `529 passed, 1 warning, 98 subtests`；Playwright `20 passed`；390/768/1440 mock 浏览器 `6 passed`，0 pageerror、0 console error；编译、Node 语法和 diff 检查通过。真实本地浏览器确认绑定账号和 15 个仓库状态；公网页面在内置浏览器直开时超时，但公网 HTTP、Pages workflow 和本地真实浏览器证据均已通过。
- 用户原有 stash 未改，仍保留 `stash@{0}: On master: 本地刷新生成的 data 快照（可丢弃，下次刷新会重新生成）`。回滚只能对精确 operation commit 使用普通 `git revert <sha>`，一次一个并复核只改配置；禁止 reset、强推或重写历史。

## 历史入口（2026-07-15）

- **2026-07-16 已完成**：订阅源二级栏目统一改为按时间平铺。布局判断已从业务筛选
  `isSubscriptionSection()` 中独立出来，微信公众号只改变列表排版，不改变条目集合、Top3、摘要、来源
  chips 或平台归类口径；8 个栏目均已在真实浏览器中验证时间流、排序、已阅/恢复与刷新缓存。
  自动化 Playwright 在本机创建 worker 时受环境 `spawn EPERM` 阻塞，未进入断言阶段；语法检查、diff 检查和
  浏览器验收通过。提交 `cbed720`，合并远端最新快照后主线提交为 `9e24f01`，已推送 `origin/master`。

- **当前状态**：`master` 已推送、与远端同步（`c45ef74` 启动微信采集按钮修复，已 rebase 到线上
  Actions 的两个 data 快照 `ce93e7e` / `553d98f` 之上）。本地 HEAD 与远程 HEAD 已核对相等。
- **已完成（2026-07-15）**：「启动微信采集」按钮全链路打通（`c45ef74`），三处坏点一并修复：
  1. `boot.js` 漏绑按钮点击事件（点了没反应）——已补 `addEventListener`。
  2. **根因**：`perform_maintenance_action` 分派设计错配——该按钮是常驻可见的，派发却依赖
     「渠道出问题才生成」的维护项列表；健康状态下列表为空，请求走到 `find_maintenance_action`
     即返回 `maintenance_action_not_found`，`start_service` 里的正确分支永远够不到。已改为给两个
     常驻 sidecar 按钮（微信 8001 / WeWe 4000）单开一条 **scope-free 无条件派发入口**
     （不传 `collection_scope`，避开签名不匹配的 TypeError），并删掉够不到的死分支。
  3. 顺手消除 WeWe RSS(4000) 按钮的同类隐患（此前只在 4000 挂掉时才可派发）。
  新增 2 个单测锁住「健康状态下两个 sidecar 按钮仍可派发」；全量 289 测试绿；HTTP API 实测
  `ok:true`；用户浏览器实测通过（进 8001 后台并成功新增「财联社」公众号）。
  计划书：`计划/一键启动微信sidecar按钮-施工计划.md`。
- **易用性（2026-07-15）**：新增双击启动器 `双击运行-刷新线上采集.cmd`，双击即弹出采集菜单，
  免去开终端敲命令。原 `刷新线上采集.ps1` 未改。注意：`.cmd` 必须存 **CRLF 换行 + GBK 编码**
  （中文文件名），LF 换行会让 cmd.exe 把 `cd /d` 拆坏导致双击闪退（本轮真踩过）。
- **历史已完成**：同步线上信源不再被本机未提交的数据快照阻塞（`70de6e8`，浏览器两轮实测通过，
  含「连续两次同步」与「线上 Actions 中途插入新提交」两个关键场景）。
  计划书：`计划/同步线上工作区脏改动阻塞rebase修复计划.md`。
- **未完成 / 待用户拍板**：
  1. **⚠️ git `stash@{0}`（备注「本地刷新生成的 data 快照」）里有 6 条工作区已丢失的
     GitHub Release 历史**（`github_foundation_sunshine_releases`，2026-07-11 快照），是这
     6 条数据的唯一备份。**严禁 drop 该 stash**。待决策：要不要恢复进 `archive.json`。
     核对方法见 `stash_cmp_report.txt`（根目录，未跟踪）。
  2. `sources.config.json` 已退为本机私有参数，但 `/api/source-config` 接口仍在。要不要彻底
     下线它，是个独立话题。
  3. `flush_pending_purge()` 用**线上配置**算存活名单，台账却也可能来自 `/api/source-config`
     的保存。该偏差**偏保守**（最坏是「该清的没清」，不会误删），随 2 的完成自然消失。
  4. 小红书 `mediacrawler_xhs` 仍不在 `ENUMERABLE_SUBSCRIPTION_SITE_IDS`（取消订阅不清历史）。
     当前无小红书数据，不紧急。
- **继续文件**：`scripts/radar/server/refresh.py`（`perform_maintenance_action` 派发，本轮已修）、
  `scripts/radar/server/collectors.py`（`start_we_mp_rss_sidecar` 实现）、`assets/js/local-collect.js`
  + `assets/js/boot.js`（按钮交互与绑定）；`scripts/radar/server/online_sources.py`（同步逻辑）、
  `scripts/radar/server/subscriptions_store.py`（清理逻辑）。
- **验收命令**：`.\.venv\Scripts\python.exe -m unittest discover tests`（289 passed）；
  单验本轮：`.\.venv\Scripts\python.exe -m unittest tests.test_local_server.LocalServerTests.test_perform_maintenance_action_can_start_we_mp_rss_without_current_issue`。
- **本机 sidecar 派发要点**：常驻可见的启动按钮（微信 8001 / WeWe 4000）必须走 `perform_maintenance_action`
  开头的 **scope-free 无条件派发字典**，不能依赖 `find_maintenance_action`（那是给「出问题才冒出来的
  修复项」用的）。新增此类按钮时照此办理，且其 handler 签名不收 `collection_scope`。
- **风险提醒**：改清理逻辑前**必读 `CLAUDE.md` 的「清理历史条目的禁区」五条**；改同步逻辑
  注意恢复工作区必须用 `git restore --source=stash@{0}`（只回写工作区），**不能用
  `git checkout stash@{0}`**（会同时写入暂存区，导致下一次同步被 `unrelated_files_already_staged`
  闸拦住——本轮真踩过，Codex 第一版就是这么写的）。

## Current State

- Date: 2026-07-14
- Local path: `E:\AI-news-reader\ai-news-radar-run`
- Branch: `master`
- **2026-07-14 同步线上信源不再被本机数据快照阻塞（提交 `70de6e8`，浏览器两轮实测通过）**：用户删订阅→保存→同步报 `online_sources_rebase_failed`。根因与删订阅无关：`sync_online_source_config` 在 commit 配置后直接 `git pull --rebase`，而 git 规定工作区有未提交的已跟踪改动就拒绝 rebase——本机采集产物 `data/*.json` 常年处于脏状态，故必然阻塞。**没用 `--autostash`**：线上 Actions 每轮都提交 `data/**`，autostash pop 时三方合并必冲突且冲突后 rebase 已完成、abort 无效，比原错误更糟。方案是「隔离→覆盖恢复」：commit 配置 → 有已跟踪脏改动才 `git stash push`（不带 `-u`）→ rebase+push → `finally` 里 `git restore --source=stash@{0} -- .` + `stash drop`。**两个真踩过的坑**：(1) Codex 第一版用 `git checkout stash@{0} -- .` 恢复，该命令会把文件同时写回工作区**和暂存区**，第一次同步成功但 data 变 staged，下一次同步被函数开头的 `unrelated_files_already_staged` 闸拦住——独立实验实锤后改 `git restore`（只回写工作区）；(2) 函数自身的 stash 操作只碰自己压入的 `stash@{0}`，用户已有的遗留 stash 会被顶到 `stash@{1}` 再自动回位，隔离实验验证无损。测试：`tests/test_local_server.py` 新增 3 个（带脏改动同步成功且 data 保持未暂存、无脏改动不建 stash、rebase 冲突时 finally 恢复且 stash 清空），同步相关 6 passed。浏览器实测两轮：改备注→保存→同步「已推送 commit 8473443」；清理备注→再同步「已推送 commit c616234」，两轮之间线上 Actions 恰好插入新快照提交 `82a474e`，rebase 真实跨过、零冲突，data 全程保持本机版未暂存、遗留 stash 6 条 GitHub Release 无损。
- **2026-07-13 两份配置口径统一 + 三个静默删数据缺口已修复（提交 `1d1a46b` 后端 + `eba3926` 前端，浏览器验收通过）**：三个缺陷同一病根——**「本轮采集哪些源」被当成了「历史里允许留哪些源」**。(1) **止血**：`cli.py` 原先用 `active_source_ids` 过滤 `archive`，导致两份配置各跑各的、互删对方独有的源——线上跑一次删光 GitHub Release（2026-07-12 真丢过 9 条），**本机跑一次会删光抖音+微信+RSS 共 174 条**（施工前用真实配置纯计算模拟实锤）；`--bilibili-only` / `--source-scope` 走同一行代码、同样会误删。已改为 `load_archive_for_collection()`，归档只由「用户明确删除源」这一显式动作清理，并删掉无调用的 `filter_archive_by_source_ids()` 及转发别名。(2) **统一名单**：本机刷新与 Actions 同读 `config/online-sources.json`（`SOURCE_CONFIG_DEFAULT_FILENAMES` 把它排到首位），`sources.config.json` 退为本机私有参数回退；GitHub Release 迁入线上名单，历史回填 10 条。(3) **补清理能力**：线上面板保存/同步也执行 purge（原先只有本机那份配置有）；**停用（`enabled` true→false）视为取消订阅**，但 `previous` 与 `current` **两边都要排除 disabled**——Codex 建议的「只排除 current」会让 `sources.config.json` 里 20 多个长期 `off` 的源被误判成「刚被删除」（实测会清掉抖音 Simon林、小红书 陈抱一），等于把同一颗雷换马甲复活；`type: rss` 补上 `opmlrss` 身份映射（身份用配置 `name`，容器型 `type: opmlrss` 不进身份表）**之后**才把 `opmlrss` 纳入 `PURGE_TRACKED_SITE_IDS`——**顺序颠倒会让存活名单为空、78 条 RSS 全灭**（实测）。(4) **采集占锁不再静默跳过清理**：原先抢不到 `REFRESH_LOCK` 就返回 `skipped` 且永不补做，等采集结束再保存时新旧配置已相同，删除记录永久丢失。现改为「算账」与「动手」分离——算删了谁是纯计算不需要锁，保存时立即算出并按并集写入 `data/pending-purge.json` 台账（已 `.gitignore`），刷新成功/失败/超时都在**释放锁之前**补做；**补做前用当前配置复核，源若被加回则只从台账划掉、拒绝清理**（安全阀，实测生效）。(5) **前端如实告知**：后端 `purged_items` 有三种形态（已清理/延后/失败），前端原先只认数字，延后时 `Number(对象)` 得 NaN 被跳过、总数算成 0、界面一个字不提；新增 `utils.js` 的 `purgedItemsNote()` 统一解读，接入三个保存入口，线上面板原先根本不读 `purged_items` 也一并补上。**数据结果**：归档 565 → 560；4 个 RSS 僵尸源（Wired AI / 宝玉 / NVIDIA / InfoQ CN，早已不在任何 OPML 里）共 20 条清除，9 个在订 RSS 源一条未损，GitHub Release 回填 10 条。首采临时用 `FIRST_COLLECT_BACKFILL_DAYS=180` 回填，长期默认 60 天规则未改。验收：pytest **326 passed**；`npm run test:e2e` 3 passed；**端到端浏览器实测**——采集进行中删除 B站 UP 主，界面显示「1 个已删除信源的历史数据将在本次采集结束后清理」，把该源加回后台账自动清空，采集结束其 19 条历史一条未损，0 控制台错误。
- **2026-07-13 信源配置迁入独立设置抽屉已上线（提交 `6134364`，本地 + 公网形态浏览器验收通过）**：首页顶栏新增「⚙ 设置」入口，原本挂在时间流上方的 `<details class="source-config-wrap">` 整块移入全屏抽屉，首页只剩时间流。**动因不只是「碍事」**：那个折叠面板名叫「信源配置」，实际塞了**四样东西**——运维工具栏（一键采集/刷新看板/重启本地服务）、本地采集状态、线上信源（写 `config/online-sources.json`，**决定公网采集什么**）、订阅成员 + 高级信源配置（都写 `sources.config.json`，**只影响本机**）。两份配置口径不一致已真出过事（2026-07-12 GitHub Release 9 条被误清）。故抽屉按语义分两个 tab：「信源」= 线上信源（带警示条「改动会同步到 GitHub」）；「本机采集」= 工具栏 + 本地状态 + 「本机私有配置」折叠区（订阅成员/高级配置，标注不影响公网）。公网静态页按 `canUseLocalBackend()` 隐藏整个「本机采集」tab。**关键约束：所有元素 id 一个没改**，故 `dom.js` 的 `getElementById` 缓存与 `local-collect.js` / `online-source-config.js` / `source-config.js` / `subscriptions.js` 全部逻辑零改动，风险只落在 HTML 挂载位置 + CSS。**顺带修掉一个既有 bug**：`.online-source-form { display: grid }` 优先级高于浏览器给 `[hidden]` 的 `display:none`，导致 `online-source-config.js` 里那句 `form.hidden = !isLocal` **一直失效**，公网访客始终能看到「保存配置」表单（填了点保存必失败）；已加 `.online-source-form[hidden] { display: none }`。**焦点陷阱踩了三次坑（Codex review 提出 P2 后修）**：判断「抽屉内哪些元素真能 Tab 到」，`offsetParent !== null` 和 `getClientRects().length > 0` **都会把折叠 `<details>` 里的按钮误判为可见**（Chrome 对折叠 details 内容仍保留 box），`closest("details:not([open])")` 也不够——「高级信源配置」的 summary **嵌套**在外层折叠的「本机私有配置」里，只查最近一层会放行。最终改为**逐层上溯所有祖先 details**（`isFocusableInDrawer()`，`settings-drawer.js`）才修对；打开时给抽屉的兄弟节点加 `inert`，关闭时还原焦点。三版错误全靠浏览器实测抓出，静态检查一版都拦不住。验收：pytest **314 passed**；`npm run test:e2e` 3 passed；Playwright 实测本地（23 个线上源真实加载、两 tab 可切、Esc 关闭、0 JS 错误）与公网静态形态（用局域网 IP 访问静态服务器复现——`canUseLocalBackend()` 白名单只认 localhost/127.0.0.1/::1/0.0.0.0，故局域网 IP 即等价公网；「本机采集」tab 隐藏、表单隐藏、同步按钮禁用、0 JS 错误）；焦点陷阱实测 Tab×40 / Shift+Tab×20 / 4 层折叠区全展开后 Tab×60 均 0 次漏出。**当时未做的「第二刀」（两份配置口径统一）已于同日完成，见上一条 `1d1a46b`。**
- **2026-07-12 取消订阅联动清理历史条目已上线（提交 `8a13a39` 首版 + `f25eb25` 修复，浏览器验收通过）**：取消订阅（删除或停用）一个 B站 UP / 抖音号后，其已采集的历史条目会在下次采集时一并清除。根因：条目的 `site_id` 是**通道级**的（`bilibili_dynamic`），订阅对象只体现在 `source`（作者名）字段，而原有清理只有 `filter_archive_by_source_ids()` 一层（按 site_id），取消通道内的单个作者时旧条目一条都掉不下去。现补上第二层 `filter_archive_by_subscriptions()`（`scripts/radar/pipeline.py`）。**三道护栏缺一不可，都是真踩过的坑**：(1) `is_online_panel_config()` 只认 `config/online-sources.json` 的 `mode=online-public-source-config` —— 仓库根 `sources.config.json` 是**容器型**格式，它的 bilibili 记录 `target` 是 `"Koji杨远骋at十字路口,夏鹏本鹏"` 这种**逗号串**，若拿它清理会把整串当一个作者名、一次删光 466 条 B站条目（首版曾用「文件名是否叫 online-sources.json」兜底，脆弱，已换成 mode 字段）；(2) `ENUMERABLE_SUBSCRIPTION_SITE_IDS`（`common.py`）只含 `bilibili_dynamic` + `mediacrawler_douyin` —— `opmlrss`（一条配置=一整个 OPML 包）、`we_mp_rss_jsonl`（一条配置=一个目录含多个公众号）、`github_*`（配置里根本没有）是容器型/遗留通道，**严禁加入**；(3) 熔断：某通道归档条目**零命中**订阅名单却要删条目时跳过清理（挡逗号串/昵称错配这类名单全错的事故），可用 `--force-subscription-cleanup` 强制放行。**熔断不可退回按「删除比例」判定**——那会让「取消一个大 UP 主」（如技术爬爬虾占 B站 43%）每轮比例不变、永远熔断、永远清不掉。抖音特殊：条目 `source` 存的是**抖音真实昵称**（`mediacrawler.py` 的 `nickname`），不等于面板备注名，故改按 `sec_uid` 精确匹配（已把 `douyin_sec_user_id` 加进 `PUBLIC_RAW_META_FIELDS` 白名单才能落库）；**存量无 sec_uid 的老条目一律保留、且不计入命中数**。前端 `assets/js/online-source-config.js` 的删除与停用都加了二次确认（停用等同取消订阅，历史同样会清）。**本地执行清理必须带 `--source-config config/online-sources.json`**，不带参数会加载 `sources.config.json` 而不做任何清理（这是设计如此，审计脚本 `scripts/audit_orphan_items.py` 默认读面板配置并会提示）。验收：pytest **304 passed**；`anno大笨蛋` 77 条 → 0 条（archive 638 → 562），B站其余 8 个 UP / 微信 3 个号 / OPML 全部 / 抖音 4 个号零误伤；Playwright 实测删除与停用弹窗文案正确、点取消后源仍在且开关复位。副作用（用户已确认接受）：GitHub Release 的 9 条被**第一层** `filter_archive_by_source_ids` 清掉——因为面板配置里没有 GitHub 源，改用面板配置跑管线后该通道整体不在启用名单里；这是两份配置口径不一致的老账，非本次功能 bug。
- **2026-07-12 微信新公众号不出现的两层根因已修复（提交 `a1ac149`，公网浏览器验收通过）**：用户在 WeRSS 后台新增「数字生命卡兹克」后一键采集不出新号。根因两层：(1) 发现层——`discover_we_mp_rss_feeds()` 走 WeRSS `GET /rss`，该端点返回**缓存 XML**（sidecar `apis/rss.py` 的 `rss.get_cache()`），新增公众号后缓存不更新，新号永远进不了发现列表 → 改走 `GET /rss/fresh`（同一 handler、`is_update=True` 直接查库、免鉴权，limit 上限 100）；(2) 读取层——`WE_MP_RSS_JSONL_DEFAULT_MAX_ITEMS=20` 是桥接 JSONL 的**全局总行数**上限而非每号 20，两号约 40 行、排序新号在前，修完发现层会变成「卡兹克出现、猫笔刀被静默截断」→ 提到 200。注意口径区分：实时通道 `WE_MP_RSS_DEFAULT_MAX_ITEMS=20` 与导出脚本 `--max-items 20` 都是**每 feed** 上限，未动也不要动。流程：Claude 写施工计划（`计划/微信新公众号发现与JSONL上限修复计划.md`）→ 用户手动把计划丢给 Codex 执行（**Codex 插件当前不可用**：CC Switch 本地代理对 SUPER NB 分组的 `/responses` 一律 403，续接/新线程/review 都被挡，恢复前只能走手动投喂）→ pytest **293 passed**（基线 292 + 新增「多账号 40 行不截断」回归测试）。验收：桥接 JSONL 40 行（两号各 20）；公网 `source-status.json` `we_mp_rss_jsonl ok=true item_count=40 max_items=200`；公网页「微信公众号 40」tab 猫笔刀与数字生命卡兹克均可见（卡兹克走首采回填），0 JS 错误。
- **2026-07-12 公网数据改为全量归档时间流（提交 `cd9bf18`，已上线验收）**：Actions 采集命令加 `--all-time`，公网 `latest-24h-all.json` 从 24 小时滚动窗口（28 条）变为全量保留归档（上线首轮 items_all=604、creator_items_all=539，`time_scope=all_time`）。归档保留天数改为 Actions Variable `ARCHIVE_DAYS`（默认 180，改数字不用改代码）。同时修掉隐患：原 `--all-time` 会完全跳过归档修剪导致无界增长，现修剪始终执行，逻辑抽为 `pipeline.prune_archive_records()`（时间兜底顺序 last_seen→published→first_seen→now 不可改动，改了会误删归档）。动因：用户在公网点的「已阅」随 24h 窗口滚动消失（已阅栏目只能从当前数据池筛已读条目）；且首采回填内容进了 archive 但公网发布文件不含窗口外内容。流程：Claude 写施工计划（`计划/公网全量时间流施工计划.md`）→ Codex 执行（4 文件、292 passed）→ Codex review 通过 → 公网浏览器验收通过（我的订阅 537、B站 461、王小七Fire 历史可见、0 JS 错误）。待用户自验：公网点几条已阅，隔一轮 Actions 刷新后确认仍在已阅栏目。注意：B站老 UP 每轮抓「最近 10 条动态」不分年代（发文少的 UP 会带出多年前动态），这是既有口径，全量视图下会可见，属预期。
- **2026-07-11 新源首采回填 60 天已实现并验收**：归档 `data/archive.json` 中从未出现过的 `(site_id, source)` 视为首采，第一次采集回填最近两个月内容，之后恢复常规口径。两层实现：(1) 管线层 `filter_raw_items_by_collect_window`（`scripts/radar/pipeline.py`）对首采源放行 60 天内全部条目（保留原「首批 5 条」兜底，60 天内不足 5 条的低频源仍有首批）；(2) fetcher 层 `cli.py collect_stage` 把归档已有源集合传给 fetcher——B站新 UP 放宽到 80 条/8 页、OPML 新 feed 不截最近 5 条、GitHub 新仓库最多取 30 个 release；抖音/公众号桥接 JSONL 只靠管线层放行（受桥接文件保留条数限制，公众号 20 条、抖音按导出文件，属客观上限）。回填窗口可用环境变量 `FIRST_COLLECT_BACKFILL_DAYS` 覆盖（缺省 60，上限 365）。状态标记：OPML feed 状态和 B站 account 状态带 `first_collect_backfill` 字段。**线上真跑后补了一刀（重要）**：Actions 命令不带 `--collect-window-hours`，管线窗口过滤在云端整体不生效，60 天边界必须在 fetcher 层落实——首采王小七Fire 时公开接口一页 20 条（含 2025 年老动态）全进了归档。已加统一截断助手 `trim_first_collect_backfill_items()`（common.py），B站两条路径、GitHub、OPML 三处复用：保留最新 5 条兜底 + 60 天窗口内其余条目，超窗历史丢弃。王小七Fire 首采多带入的约 15 条超窗老动态仍留在线上归档（无害，按时间排序沉底）；如要清掉，可在面板删除该源（联动清理历史数据）再重新添加，重新首采即按 60 天口径。验收：pytest 全量 287 passed（基线 284 + 新增 3）；scratch 真实对照——全新 RSS 源首采 30 条全入库（旧行为 5 条）、第二轮恢复 5 条常规口径；本地页面真实「刷新看板数据」+ 浏览器回归通过，老源零误回填（15/15 窗口外正确跳过）。
- **2026-07-11 产品方向调整：从「AI 新闻精选雷达」转为「个人订阅聚合器」**。核心价值是聚合用户自己的订阅源（B站/抖音/小红书/微信公众号/YouTube/RSS/GitHub），AI 相关性不再是筛选标准。AI 打分算法（`scripts/ai_relevance.py`）保留但不再作为默认筛选器：阈值改为读环境变量 `AI_RELEVANCE_THRESHOLD`（缺省 0.65），**GitHub Actions Variable 已设为 `0`**，即线上不过滤、主榜等于全量（实测主榜由 1 条变 28 条）。删掉该 Actions 变量即可恢复默认筛选。已同步 `CLAUDE.md` / `AGENTS.md` / `skills/ai-news-radar/SKILL.md` / `docs/SOURCE_COVERAGE.md` 四处开工必读文档，避免 AI 协作按旧方向决策。提交 `e87d87f`（开关）、`32b7554`（文档）。
- **2026-07-11 线上信源面板静默清空事故（已修复，提交 `687f385`）**：用户在面板加一个 B站 UP，生成的提交把 `config/online-sources.json` 从 20 个源删到只剩 1 个；仅因 push 撞上 Actions 快照提交被拒才没上线。三层根因：(1) 接入 `we_mp_rss_jsonl` 时漏了把新类型加进 `scripts/radar/server/online_sources.py` 的 `ONLINE_ALLOWED_TYPES` 白名单 → `normalize_online_source_record()` 抛错 → **整份线上配置读取失败**（`/api/online-source-config` 返回 `ok:false`）；(2) 前端 `assets/js/online-source-config.js` 拿到 `ok:false` 后走 `{ sources: [] }` 空列表兜底，保存即全量覆盖；(3) `sync_online_source_config()` push 前未 `pull --rebase`，必然撞 Actions 快照提交。三层修复均已浏览器实测：白名单补齐（`locator` 允许留空）；前端区分「已加载/未加载」，未加载成功时禁用保存与同步；后端加批量删除护栏（源数骤降过半拒绝写盘，需 `confirm_bulk_delete`）；push 前先 `pull --rebase`，失败中止绝不强推。**新增源类型的必查清单已写入 `CLAUDE.md`。**
- 2026-07-11 线上信源增补：B站 UP `罗永浩的十字路口`(538596213)、`中二的大暄哥`(28756625)；YouTube 频道 `小岛大浪吹`、`脑总MrBrain`、`HighLevelz`。当前线上共 24 个用户可编辑源（另有 1 条内部 OPML wrapper，UI 隐藏，故面板显示 24）。**注意：油管订阅原先只存在于私有的 `feeds/follow.opml`（`.gitignore` 排除，Actions 读不到），所以公网油管 tab 长期为 0——这不是 bug；要上公网必须写进 `config/online-sources.json`。** 另注意 24h 窗口对低频源（官方 AI 博客普遍 3~10 天一篇）过于苛刻，主榜曾长期近空，这是选源与窗口不匹配，非抓取故障。
- 2026-07-11 微信公众号（WeRSS）接入公网自动刷新已上线并通过公网页面 + 人工验收：架构与抖音同构——本机 WeRSS sidecar 定时采集 → `scripts/export_we_mp_rss_jsonl.py` 导出仅含公开字段的 JSONL（标题/链接/时间/账号/摘要，不含 cookie 或授权态）→ 私有桥接仓库 `kunkunzi996/wechat-bridge`（`output/wechat/jsonl/wechat_contents_latest.jsonl` + `manifest.json`）→ GitHub Actions 用只读 SSH 部署密钥克隆并以 `WE_MP_RSS_JSONL_DIR` 读取。新增 `we_mp_rss_jsonl` 平行源类型，**不改动已有的实时 `we_mp_rss` 通道**，两条通道并存。本机采集脚本 `deploy/local/collect-wechat-and-push.ps1` 作为**第二个动作**追加到已有计划任务 `DouyinCollectAndPush`（10:00/15:00/21:00，未新建任务、未动抖音动作），桌面手动入口 `wechat-collect-now.cmd`，日志 `E:\AI-news-reader\wechat-collect.log`。Actions 侧配置 vars `WECHAT_BRIDGE_REPO` + secret `WECHAT_BRIDGE_SSH_KEY`（只读部署密钥 `actions-readonly`，实测 clone 通、push 被拒）。本机推送 bridge 用用户自己的 GitHub 凭证。
  施工计划漏了两层，真跑才暴露，**新增桥接类数据源时必查**：(1) `scripts/radar/cli.py:795` 按 `active_source_ids` 过滤 raw_items，RawItem 的 `site_id` 必须等于 `config/online-sources.json` 里启用的源 id（前端归一靠 `site_name` 同名，不能靠复用别的通道 id）；(2) 前端 `assets/js/dom.js` 的 `SUBSCRIPTION_SITE_IDS` 要加新 site_id，且 `HIDDEN_PLATFORM_IDS` 原本整体隐藏了 `wechat` 平台（弃用 wewe_rss 的历史遗留），已解除；`HIDDEN_SOURCE_IDS` 里的 `wewe_rss` / `maobidao_wudaolu_backup` 保持隐藏。另：Windows PowerShell 5.1 读**无 BOM** 的 `.ps1` 会把中文字面量按 GBK 读成乱码，新建 `.ps1` 必须存为 UTF-8 **带 BOM**。
  线上验收：公网页面出现「微信公众号」标签，「我的订阅」27 → 28 条，猫笔刀条目可见且链接指向真实 `mp.weixin.qq.com`。注意 24h 窗口内该公众号通常只有 1 篇（日更节奏），本机 JSONL 每号保留最近 20 条（导出侧每 feed 20 条；读取侧全局上限 2026-07-12 已从 20 提至 200）。
- 2026-07-10 抖音订阅接入公网自动刷新已上线并通过公网页面验收：架构为本机 MediaCrawler 定时采集 → 私有桥接仓库 `kunkunzi996/douyin-bridge`（仅 `output/douyin/jsonl/creator_contents_latest.jsonl` + `manifest.json`）→ GitHub Actions 用只读 SSH 部署密钥克隆并以 `MEDIACRAWLER_LOCAL_DIR` 读取。功能提交 `12c532b`、部署提交 `d099cf0`。线上同步白名单 `scripts/radar/server/online_sources.py` 新增 `mediacrawler_jsonl` 抖音类型（主页链接清洗为 `https://www.douyin.com/user/<sec_uid>`），面板"新增入口"支持"添加抖音博主"；`config/online-sources.json` 已启用 4 个博主（珍妮丁丁说AI/Simon林/FredTalk/匿名用户996）。本机计划任务 `DouyinCollectAndPush` 每天 10:00/15:00/21:00（错过开机补跑），桌面《抖音采集并上线.cmd》手动入口与定时共用链路，日志 `E:\AI-news-reader\douyin-collect.log`。Actions 侧配置 vars `DOUYIN_BRIDGE_REPO` + secret `DOUYIN_BRIDGE_SSH_KEY`（私钥必须经 bash 原始重定向写入——首次经 PowerShell 管道写入被行尾污染导致 `Permission denied (publickey)`，已换部署密钥 v2 修复）。另修复采集脚本 PS5.1 下 git stderr 被 `ErrorActionPreference=Stop` 误判异常的问题。线上验收：run `29085310569` 后 `source-status.json` 报 `mediacrawler_douyin ok=true item_count=32`、4 博主全 ok，公网页面出现"抖音 1"标签、Simon林 作品可见、`4/4 源正常`。注意：线上 `latest-24h-all.json` 长期为 0 条属既有常态（与本轮无关）。方案与运维文档见 `docs/guides/douyin-cloud-pc-automation.md`；迁移云电脑时按指南搬三个仓库即可，线上零改动。
- 2026-07-10 online source self-service acceptance completed: the user added Bilibili UP `夏鹏本鹏` (`3546800797518640`) from the local `127.0.0.1:8080` online-source panel and confirmed the full flow works. The panel created and pushed `10c9c05 配置：同步线上信源`; GitHub Actions then created remote snapshot commit `b268f77 chore: update ai news snapshot`. Remote `data/source-status.json` reports the new UP `ok=true`, `item_count=10`, `fetch_mode=cookie_full_dynamic`, and `generated_at=2026-07-10T04:36:10.874892Z`. The public UI now exposes 14 editable public source records (the internal OPML wrapper is hidden). For cloud collection, `config/online-sources.json` is authoritative; the local “订阅成员” / `sources.config.json` remains a private local collection configuration.
- 2026-07-10 scheduled refresh reliability update: the page stayed at `07/10 08:09` because the last successful GitHub Actions snapshot was generated at `2026-07-10T00:09:30Z`; the page clock was correct, but later scheduled runs were delayed or skipped. `.github/workflows/update-news.yml` now uses `7,37 * * * *` instead of `*/30 * * * *` to avoid the busiest start-of-hour window. `workflow_dispatch` and non-data pushes to `master` remain available as immediate triggers.
- 2026-07-10 online source config MVP completed and deployed: added public online source config files `config/online-sources.json` and `feeds/online-sources.opml`, switched GitHub Actions to `--source-config config/online-sources.json`, added local-only APIs `GET/POST /api/online-source-config` and `POST /api/sync-online-source-config`, and added the “线上信源” panel. Feature commit `88abe46 功能：新增线上信源一键同步` was pushed; the public page reads the real public config in read-only mode. Verification passed: `py_compile`, `node --check`, 225 tests, local Playwright save flow, public browser rendering, and public `source-status.json` reporting `source_config.active=true` / `source_scope=configured_sources`. Do not include generated `data/*.json`, `sources.config.json`, `feeds/follow.opml`, `local-secrets/`, or `计划/` review files in normal feature commits.
- 2026-07-09 GitHub Pages minimal上线已完成并验收：Pages 功能提交 `aa13415 上线：准备 GitHub Pages 静态站` 已推送到 `origin/master`；公网 `https://kunkunzi996.github.io/ai-news-radar/` 返回 200，页面正常显示 `AI News Radar` 和 `静态数据`；`https://kunkunzi996.github.io/ai-news-radar/data/source-status.json` 返回 200，当前 `3/3` 源正常；用户截图已确认公网页面显示 `静态数据` / `3/3 源正常`。本轮后续只做交接文档保存，不提交本地 `data/*.json` 生成产物、`计划/` 历史文件或任何私密文件。
- 2026-07-09 stable remote-data baseline: GitHub Actions cloud refresh and local remote-data display have both passed acceptance. Remote `data/source-status.json` refreshed at `2026-07-09T13:44:44Z` and reports `3/3` healthy sites: `github_foundation_sunshine_releases`, `bilibili_dynamic`, and `opmlrss`. Local browser acceptance on `http://127.0.0.1:8080/?dataBase=https%3A%2F%2Fraw.githubusercontent.com%2Fkunkunzi996%2Fai-news-radar%2Fmaster%2Fdata%2F` showed the `远程数据` badge, `3/3 源正常`, and no visible `公众号 / WeWe RSS / 猫笔刀` text.
- 2026-07-09 WeChat public-account default-source retirement completed: commit `11bd3b1 移除：默认公众号采集源` removed `wewe_rss` and `maobidao_wudaolu_backup` from the default `tested_creator_sources` cloud scope, retired their default source-config seed records, filters old browser/local drafts through `RETIRED_SOURCE_CONFIG_IDS`, and updated `README.md` plus `docs/SOURCE_COVERAGE.md`. Old WeWe RSS / backup-site code remains only for explicit local experiments.
- 2026-07-09 local Git baseline cleaned: local `master` now matches `origin/master` at `d0ee862 chore: update ai news snapshot`. The previous local-only OPML trigger commit is preserved at backup branch `backup/local-opml-trigger-20260709-80fe98f`. Remaining local dirty files are intentionally not part of the stable baseline: `data/*.json` generated snapshots, an old uncommitted `HANDOFF.md` addition, and untracked review/plan files under `计划/`.
- 2026-07-09 cloud auto-refresh trigger update: `.github/workflows/update-news.yml` also runs on pushes to `master` while ignoring pure `data/**` changes, so code/config/documentation pushes can immediately refresh the remote JSON snapshot. The schedule was later moved to `7,37 * * * *` on 2026-07-10; `workflow_dispatch` remains available.
- 2026-07-09 remote data display mode: local dashboard can now read generated `data/*.json` from a remote data directory without moving the whole site online. Add `?dataBase=https://example.com/path/to/data/` to the local page URL; the browser stores the remote base in localStorage, shows a `远程数据` pill in the hero, and falls back to local `data/*.json` if the remote read fails. Use `?dataBase=local` to return to local data mode. This only changes frontend JSON loading; it does not change collection scripts, GitHub Actions, MediaCrawler, cookies, or private config.
- 2026-07-09 server auto-run deployment prep: added a safe Linux deployment runbook at `docs/guides/server-deployment.md` plus templates under `deploy/` for systemd refresh timer, Nginx static serving, and `/etc/ai-news-radar.env` environment variables. Recommended production shape is: systemd runs `scripts/update_news.py` every 30 minutes, generated `data/*.json` stays in the repo data dir, and Nginx exposes only `index.html`, `assets/`, `site.webmanifest`, and `data/*.json`. Do not expose `scripts/local_server.py` to the public internet; keep real cookies, API keys, private OPML, `sources.config.json`, and login state outside Git.
- 2026-07-07 project-level agent rules updated: `AGENTS.md` and `CLAUDE.md` now include two iron rules. Every bug fix must start from first-principles root-cause analysis before editing, including the bottom-level fact/root cause, whether architecture/schema/API changes are actually required, and the smallest reversible fix. Every acceptance/test for browser-visible flows, local dashboards, or UI interactions must use a browser tool for real validation before reporting; if browser validation is impossible, the blocker and unverified items must be stated.
- 2026-07-07 code structure split stage C completed: followed `计划\代码结构分层拆分实施计划.md` stage C only. The old monolithic `assets/app.js` was split into classic browser scripts under `assets/js/` (`dom.js`, `utils.js`, `render.js`, `subscriptions.js`, `source-config.js`, `local-collect.js`, `boot.js`), and `index.html` now loads those seven files in order with cache version `js-split-0707b`. `assets/app.js` was removed after the split checks. Function count reconciled exactly: baseline `计划\app_js_函数清单.txt` has 308 entries, split modules also have 308. Follow-up fixed the one-click collect regression by restoring the missed `ONE_CLICK_PLATFORM_TIMEOUT_MS` and `ONE_CLICK_POLL_MS` constants in `assets/js/local-collect.js`; after the user still saw the error, the script cache version was bumped from `js-split-0707a` to `js-split-0707b` so the browser no longer reuses the stale pre-fix file. Verification passed: `node --check` for all split JS files, full unittest discover passed 218 tests, `git diff --check` passed with only the existing Windows LF-to-CRLF warning for `index.html` / `PROJECT_STATE.md`, and Playwright on `http://127.0.0.1:8099/` rendered the page with console `Errors: 0, Warnings: 0`; browser eval confirmed `ONE_CLICK_PLATFORM_TIMEOUT_MS=720000`, `ONE_CLICK_POLL_MS=3500`, and `waitForCollectorDone` is available. Real browser-plugin acceptance on `http://127.0.0.1:8080/` then clicked `一键采集`: the button entered `一键采集中...`, Douyin collector ran and completed with `item_count=11`, XHS collector ran and completed with `item_count=0`, refresh progress reached `completed/100% 刷新完成`, the button returned to `一键采集`, and browser logs had no errors/warnings. The final `source-status.json` still reports `mediacrawler_douyin_no_items` / `mediacrawler_xhs_no_items` for the current `自上次采集` window (`fetched_raw_items=0`), so the remaining maintenance display is a data/window result, not the Stage-C JavaScript ReferenceError. Stage C is not committed yet; do not include generated `data/*.json` changes in the stage-C commit unless the user explicitly asks.
- 2026-07-07 code structure split stage B completed: after committing stage A as `a680bb3 refactor: split update news pipeline`, followed `计划\代码结构分层拆分实施计划.md` stage B only. `scripts/local_server.py` is now a facade plus `LocalRadarHandler` and `main()`, while moved helpers live under `scripts/radar/server/` (`cdp.py`, `maintenance.py`, `subscriptions_store.py`, `collectors.py`, `refresh.py`). Function/class count reconciled exactly: baseline `计划\local_server_函数清单.txt` has 108 entries, split modules plus facade also have 108. Updated only the stage-B test patch path to `scripts.radar.server.cdp.active_bilibili_cdp_port`. Verification passed: `py_compile` for local_server facade and all new server modules, `python -m unittest tests.test_local_server -q` passed 65 tests, full unittest discover passed 218 tests, and direct `.\.venv\Scripts\python.exe scripts\local_server.py --help` exited 0. Stage C not started.
- 2026-07-07 code structure split stage A completed: followed `计划\代码结构分层拆分实施计划.md` stage A only. `scripts/update_news.py` is now a 54-line facade/entry point with the direct-run path shim, and the moved implementation lives under `scripts/radar/` (`common.py`, `config_runtime.py`, `pipeline.py`, `cli.py`, and `fetchers/*`). Function/class count reconciled exactly: baseline `计划\update_news_函数清单.txt` has 229 entries, split modules also have 229. Updated only the two stage-A test patch paths (`scripts.radar.fetchers.subscriptions.requests.get` and `scripts.radar.fetchers.public.*`). Verification passed: `py_compile` for update facade and all new radar modules, targeted unittest for `tests.test_private_bridge_sources tests.test_topic_filter` passed 128 tests, full unittest discover passed 218 tests, and direct `.\.venv\Scripts\python.exe scripts\update_news.py --help` exited 0. Stage B/C not started.
- 2026-07-07 workspace redundancy cleanup completed: executed the git hygiene and explicit-path cleanup from `E:\AI-news-reader\计划\项目结构梳理与冗余清理计划.md`. Added local excludes for `MediaCrawler-local-test` and `wewe-rss-sidecar`, added `data/title-zh-cache.json` to `.gitignore`, and ran `git rm --cached data/title-zh-cache.json` while keeping the local file. After the user closed the MediaCrawler Chrome profile, deleted the verified old clone `E:\AI-news-reader\ai-news-radar`, the outer broken `.git`, empty `.agents`/`.codex`, and only the listed Chrome cache/model directories. Preserved login-state files including `chrome-profile\Default\Network\Cookies`, `Default\Login Data`, and `Local State`. Verification: main repo unittest discover passed 218 tests, `node --check assets/app.js` passed, MediaCrawler status only shows the three intentional modified source files, and wewe-rss status is clean.
- 2026-07-07 Douyin anonymous-account follow-up: after restarting the 8080 server and rerunning `启动抖音采集`, the fixed startup command passed all 4 enabled Douyin creators as pure `sec_uid` values, including `匿名用户996`. MediaCrawler logs confirmed it parsed Simon林, 珍妮丁丁说AI, FredTalk, and `匿名用户996`; the 12:28 failure was also addressed by protecting local CDP discovery from `ALL_PROXY=socks5://...` in `scripts/run_mediacrawler_douyin.py`. The final MediaCrawler creator export contains `匿名用户996` in `creator_creators_2026-07-07.jsonl` with `videos_count=0`, but `creator_contents_2026-07-07.jsonl` has no rows for that account. After `刷新看板数据`, `data/source-status.json` reports `匿名用户996 item_count=0` / `mediacrawler_douyin_no_items`. Current conclusion: the project bridge is now reaching the account; the missing item is because the collector-visible Douyin profile has no public videos. Next check should be on Douyin visibility/approval/privacy or whether the video is visible from the MediaCrawler Chrome profile.
- 2026-07-07 local server restart button: added a local-only `重启本地服务` button in the source-config/local-collection toolbar. `scripts/local_server.py` now exposes `POST /api/restart-local-server`, rejects non-local origins through the existing origin guard, refuses to restart while a dashboard refresh is running, and restarts by launching a tiny helper process that reopens the current Python command after the old 8080 process exits. This avoids the Windows `C:\Program Files` path parsing failure seen with direct `os.execv`. `assets/app.js` calls the endpoint, shows restart status, waits for `/api/local-status` to come back, then reloads the page. `index.html` adds the button and bumps the app cache version to `local-server-restart-0707a`. Verification: `py_compile scripts/local_server.py`, `node --check assets/app.js`, and `python -m unittest tests.test_local_server -q` passed with 64 tests. Real endpoint acceptance also passed: `POST /api/restart-local-server` returned 202, 8080 moved from PID 28940 to PID 30548, and `/api/local-status` returned OK after restart.
- 2026-07-07 Douyin new-account collector fix: user added Douyin test account `匿名用户996`, but after collection it did not appear. Root cause was the local collector startup path, not the Radar JSON bridge: `sources.config.json` had 4 enabled Douyin records and `data/source-status.json` reported `匿名用户996 item_count=0`, while the latest MediaCrawler export `creator_contents_2026-07-07.jsonl` only contained Simon林 and 珍妮丁丁说AI. `scripts/local_server.py` previously returned the first enabled Douyin homepage URL from `mediacrawler_creator_id()`, so MediaCrawler never received all enabled creators. It now joins all enabled homepage URL locators and passes the comma-separated creator list to MediaCrawler, which already supports multiple `--creator_id` values. Verification: `py_compile scripts/local_server.py`, `node --check assets/app.js`, `python -m unittest tests.test_local_server -q` passed; dry-run now reports `creator_count=4` and includes the `匿名用户996` creator prefix. Next real acceptance: restart the 8080 local server if it is already running, click `启动抖音采集`, then `刷新看板数据`, and confirm `data/source-status.json` gives `匿名用户996 item_count>0` if the video is public/visible to the collector profile.
- 2026-07-07 WeChat public-account feature pause: user decided to hide 微信公众号 features for now because the subscription source is not stable. `assets/app.js` now keeps a reversible hidden-platform list for `wechat` and hidden source ids `wewe_rss` / `maobidao_wudaolu_backup`; the homepage tabs, subscription manager tabs, source-config filters/list/summary, local collection overview, source-health counts, maintenance issues, site filters, and rendered items ignore those hidden WeChat sources. `sources.config.json` also sets `wewe_rss_maobidao.enabled=false`, so future local refreshes do not try the WeWe RSS sidecar by default. This is a UI/config pause, not a source deletion.
- 2026-07-06 subscription time-filter fix: user reported that after selecting `过去 24 小时`, B站 and 油管 still showed all subscription history. Root cause was frontend-only: `setActiveSection()` forced `state.timeRangeFilter` back to `all` whenever the active section was a subscription section. The data path already had `applyTimeRange()` for subscription sections, so the fix removes that forced reset and bumps the `assets/app.js` cache version. Verified on `http://127.0.0.1:8080/`: default all-mode tabs showed `我的订阅326 / B站279 / 油管10`; after selecting 24h, tabs showed `我的订阅1 / B站1 / 油管0`; entering B站 and 油管 kept `timeRangeSelect=24h`.
- Latest pushed commit before cleanup: `7477fff fix: separate collection window from archive view`
- Task type: Subscription read-state feature cleanup / Git save
- Server: `http://127.0.0.1:8080/`
- 2026-07-06 read-state title collision fix: user reported marking one Bilibili `技术爬爬虾 / 分享动态` card as read moved all same-title cards to `已阅`. Root cause was that read-state reused broad event/title identity keys (`title:分享动态` / `event:title:分享动态`) that are useful for story matching but too coarse for per-card read state. `assets/app.js` now separates read tracking from story identity: read-state uses strong per-item keys (`url`, backend `id`, `bilibili_dynamic_id`, `bilibili_opus_id`) first, and only falls back to a non-generic title key when no strong key exists. Real data check: `技术爬爬虾 / 分享动态` has 59 records and 59 unique strong keys.
- 2026-07-06 source-config stale draft fix: user reported Douyin/Xiaohongshu disappeared after refreshing sources. Root cause was not MediaCrawler capture failure: the latest local JSONL files existed, but the current browser/source-config draft wrote an older `sources.config.json` before refresh, leaving `mediacrawler_douyin` and `mediacrawler_xhs` disabled and Bilibili reduced from 4 UPs to 2. `assets/app.js` now preserves the original draft `updated_at` when only seed/catalog merging changes a browser draft, so stale browser drafts cannot become artificially newer than the disk config. Local `sources.config.json` was restored to enable OPML, 4 Bilibili UPs, Douyin subscriptions, Xiaohongshu, WeWe RSS, and GitHub Release. Real refresh at `2026-07-06T13:31:03Z` restored `enabled_site_ids=bilibili_dynamic,github_foundation_sunshine_releases,mediacrawler_douyin,mediacrawler_xhs,opmlrss,wewe_rss`; `mediacrawler_douyin.ok=true item_count=5`; `mediacrawler_xhs.ok=true item_count=5`; HTTP `latest-24h-all.json` has 5 Douyin and 5 Xiaohongshu items.
- 2026-07-06 B站新订阅首批入库 + 已阅状态稳定性修复：根因是 `自上次采集` 窗口会过滤发布时间较旧的新 UP 动态，导致刚新增的 B站 UP 虽然抓到了原始候选，但未进入归档和页面数据；现在采集窗口过滤会对归档里从未出现过或首批不足 5 条的订阅对象补齐首批内容，老订阅仍按窗口过滤。前端 `已阅` 也从单一身份 key 改为多 key 匹配/写入，包含 URL、后端 id、B站动态 id / opus id、标题事件 key，降低刷新后同一条内容识别失败的概率。真实刷新后 `清华姜学长` 已进入 `data/archive.json` 和 `data/latest-24h-all.json`，归档首批 5 条；用户已在本地页面验收成功。注意：如果浏览器在 `localhost:8080` 和 `127.0.0.1:8080` 之间切换，浏览器本身仍会把 localStorage 当成两个站点；推荐统一使用 `http://127.0.0.1:8080/`。
- 2026-07-06 local collection progress update: `刷新看板数据` now starts a background refresh task and exposes `/api/refresh-progress` for frontend polling. The local collection panel renders a compact progress bar with current step, percent, rough remaining time, and recent step logs. `一键采集` also writes visible step logs for Douyin completion, Xiaohongshu completion, and the final dashboard refresh handoff. Follow-up fixes keep the progress panel visible while Douyin/XHS local polling receives backend `idle` progress, and hide stale `completed` progress after a normal page reload. This is a UI/status improvement only; it does not change collector source logic, secrets, or `data/*.json` semantics.
- 2026-07-06 local status passthrough fix: `/api/local-status` now preserves `collection_window_hours`, `raw_items_before_collection_window`, `skipped_collection_window_items`, and each site's `raw_item_count` / `window_item_count` / collection cap metadata from `data/source-status.json`. This fixes the collection overview showing only raw counts even when the status file already contains window counts.
- 2026-07-06 subscription collection cap update: GitHub Release remains capped at the latest 5 releases, B站动态 now defaults to the latest 5 items per account instead of 20, and OPML/YouTube feeds now keep the latest 5 entries per feed before the normal `--collect-window-hours` filter runs. `source-status.json` site rows now expose `raw_item_count`, `window_item_count`, and `collection_window_hours`, so the local collection overview can show `窗口 X / 原始 Y` for YouTube/B站/GitHub-style sources after the next refresh.
- 2026-07-06 local collection hierarchy update: `订阅源采集概览` now groups enabled sources by platform first (`YouTube 订阅`、`B站动态`、`抖音`、`小红书`、`微信公众号`、`GitHub Release`) and exposes concrete subscriptions as expandable second-level rows. YouTube second-level rows are loaded from `/api/subscriptions/youtube`; B站 rows are split from the unified Bilibili source target; Douyin/XHS/WeChat/GitHub rows continue to use their source records and existing local collector/status data.
- 2026-07-05 local collection overview update: the local collection panel now renders one compact `订阅源采集概览` card for all enabled subscription sources, using `/api/local-status` `source_config.enabled_sources`, `source_status.sites`, maintenance issues, and MediaCrawler collector summaries. It avoids one card per source; Douyin/Xiaohongshu rows keep their platform-level collector actions and counts, while WeWe rows surface their per-feed maintenance issue when available.
- 2026-07-05 collection-window update: the local `24h` collection scope is now reader-facing as `自上次采集` and no longer passes a fixed `--collect-window-hours 24` to `scripts/update_news.py`. `scripts/local_server.py` reads the previous `data/source-status.json.generated_at` before launching refresh, converts elapsed time to whole hours with ceil, and falls back to 24 hours when the timestamp is missing, invalid, or in the future. `collection_scope=all` still omits `--collect-window-hours`. MediaCrawler startup still keeps its fixed 24h stats argument by design.
- 2026-07-05 collector-card wording cleanup: MediaCrawler collector cards now label the filtered count as `窗口作品` and the JSONL total as `原始写入`, instead of exposing `24h作品`. `窗口作品` is a collector-side time-window hit count from the latest raw JSONL; final dashboard additions can still be lower after the normal Radar refresh path dedupes, validates timestamps, merges archive data, and applies display filters.
- 2026-07-05 read-state update: subscription cards in `我的订阅` and platform subscription tabs now support a browser-local `已阅` action. Marked items are hidden from normal subscription views and appear in the new `已阅` tab, where `恢复` moves them back. The state is stored only in browser `localStorage` under `ai-news-radar-read-items-v1`; it is not written to `data/*.json`, `sources.config.json`, or the local server. Review follow-up fixed the button eligibility path so every card rendered from the current subscription pool can show `已阅/恢复`, even if `creator_items_all` came from the backend's wider subscription detection than the frontend `isSubscriptionItem()` helper. Read identity now reuses the existing `itemIdentityKey()` helper.
- Current local view: the source configuration panel has been upgraded into a local collection console. The page now has a local collection status panel, an `执行采集` button, a `检查状态` button, and maintenance action buttons. `scripts/local_server.py` exposes `GET /api/local-status` so the page can show maintenance hints from `data/source-status.json` and local read-only probes without reading cookies, login state, WeWe RSS database files, or MediaCrawler browser profiles.
- 2026-07-04 collection scope update: local `执行采集` now has an explicit range selector with two modes, `过去24小时` (`collection_scope=24h`, default) and `全量` (`collection_scope=all`). The default command uses `--collect-window-hours 24 --all-time`: only newly accepted raw items are limited to a real 24h `published_at` window, while the generated page still publishes the full retained archive (`old data + newly collected 24h data`). `/api/refresh` only accepts this whitelist and still builds a fixed project command, so the frontend cannot pass arbitrary script arguments. The same scope is now also sent to Douyin/Xiaohongshu MediaCrawler maintenance starts: 24h mode passes `--collect-window-hours 24` into the runner, caps the creator crawl to the latest 5 notes/videos per creator by default, and writes a `mediacrawler-<platform>-collection-window.json` summary so the collector card can show 24h hits separately from raw JSONL rows; the raw `creator_contents_*.jsonl` is preserved. If the newest JSONL is 0 bytes, the reader now falls back to the newest non-empty sibling file.
- Local maintenance hints currently cover failed sources, zero-item sources, missing Bilibili cookie/full-dynamic fallback, failed WeWe RSS feeds, unreachable local WeWe RSS sidecar, missing WeWe feed ids, missing/empty/stale MediaCrawler JSONL files, invalid `sources.config.json`, and missing source-status output. Maintenance cards can now open whitelisted repair surfaces such as WeWe RSS dashboard/QR login, Bilibili login, platform pages, or the configured JSONL folder; when the local WeWe RSS sidecar is down, the action can start the fixed local sidecar before opening the dashboard.
- Bilibili maintenance now has a local small-account cookie workflow: the card starts a dedicated Chrome/Edge profile under `local-secrets/bilibili-profile`, can sync Bilibili cookies from that local CDP profile into `local-secrets/bilibili-cookies.txt`, and `/api/refresh` injects that file as `BILIBILI_COOKIE_FILE` for the fixed refresh command. It does not reuse the user's daily browser profile and does not expose cookie content in the UI.
- Final Bilibili acceptance passed after user ran `执行采集`: local status reported `cookie_present=true`, `fetch_mode=cookie_full_dynamic`, `ok=true`, `item_count=40`, and `error=null`.
- Douyin and Xiaohongshu maintenance now have fixed local actions to start `E:\AI-news-reader\MediaCrawler-local-test` in MediaCrawler creator mode through `scripts/run_mediacrawler_douyin.py`. The runner launches or reuses a dedicated Chrome CDP profile under `MediaCrawler-local-test\chrome-profile` instead of attaching to the user's everyday browser. The local collection panel now shows plain-language collector progress cards with running/completed state, JSONL line count, latest output time, and the next action. Xiaohongshu start infers the creator profile URL from existing JSONL `user_id` when available, or from `MEDIACRAWLER_XHS_CREATOR_ID`. The normal radar refresh still only reads JSONL files; generated dated JSONL files are resolved by newest non-empty `creator_contents_*.jsonl` in the configured folder so old date paths or accidental empty files do not keep the radar stuck on stale data. In scoped collection mode, the collector card displays `窗口作品` plus `原始写入`, so `0` means no current time-window hit rather than raw export deletion.
- Source management now supports local UI filters for all/enabled/needs-maintenance and key platforms: WeChat public accounts, Xiaohongshu, Douyin, Bilibili, RSS, and GitHub. Maintenance cards can locate the matching source config record.
- The subscription member panel now supports WeChat public-account member add/delete through WeWe RSS source records. Deleting the default `wewe_rss_maobidao` member records its source id in `deleted_source_ids`, so it will not be silently re-added by the built-in seed merge after save/reload. The old `maobidao_wudaolu_backup` public backup source is no longer treated as a WeWe RSS feed just because its channel contains `微信公众号`.
- The WeChat subscription panel now has a `同步 WeWe RSS` button. The local server exposes `GET /api/wewe-rss/feeds`, which only reads the local WeWe RSS `/feeds` JSON endpoint and returns sanitized `id/name/intro/updateTime/syncTime` fields. The button syncs those feeds into WeChat subscription source records and writes `sources.config.json`, so users no longer need to manually copy feed ids.
- Homepage content tabs have been reworked from topic categories to subscription/platform categories. The top tab bar now defaults to `我的订阅` and only shows `我的订阅`、`抖音`、`小红书`、`微信公众号`、`B站`、`油管`; platform tabs are derived from subscription item source ids, source names, URLs, and titles.
- 2026-07-05 subscription stream layout update: `我的订阅` and platform subscription tabs now render as a flat timeline instead of nested source/platform groups. The page still keeps source/platform filter pills, but the reading list itself is ordered by the active sort mode; default `时间` sort puts the newest subscription items first across GitHub, WeChat, Douyin, Xiaohongshu, Bilibili, and YouTube.
- The former `TODAY'S SIGNALS` / `今日重点信号` block has been removed from the homepage after checking that only Douyin and Xiaohongshu currently provide comparable interaction metrics. The page now flows from source configuration directly into WaytoAGI/community updates and the main list.
- Source group headings and source filters now use reader-facing platform names such as `微信公众号`、`YouTube`、`GitHub`、`小红书`、`抖音` and `B站` instead of adapter names like `WeWe RSS` or `MediaCrawler Xiaohongshu`.
- User acceptance passed on 2026-07-04 after local WeWe RSS and AI News Radar services were restarted: WeWe RSS account `547013436` is enabled, 猫笔刀 shows `诚实回答` (`2026-07-03 22:24:51`), `http://127.0.0.1:8080/` returns HTTP 200, and `/api/local-status` returns `ok=true`.
- WeChat subscription-member add/delete verification on 2026-07-04: `node --check assets\app.js`, `py_compile scripts\local_server.py scripts\update_news.py`, and `python -m unittest tests.test_local_server tests.test_topic_filter` passed; in-app browser check at `http://127.0.0.1:8080/` confirmed `assets/app.js?v=wechat-subscription-members-0704a` and the `微信公众号` subscription tab shows `猫笔刀 / MP_WXS_3198966508`.
- WeWe RSS feed-list sync verification on 2026-07-04: `GET /api/wewe-rss/feeds` returned `猫笔刀 / MP_WXS_3198966508`; `node --check assets\app.js`, `py_compile scripts\local_server.py scripts\update_news.py`, and `python -m unittest tests.test_local_server tests.test_topic_filter` passed; browser check confirmed `assets/app.js?v=wewe-rss-sync-feeds-0704a`, the `同步 WeWe RSS` button is visible on the `微信公众号` tab, and clicking it reports `已同步并保存 1 个公众号`.
- Read-state verification on 2026-07-05: `node --check assets/app.js` passed; `git diff --check -- assets/app.js HANDOFF.md` passed with only Windows LF/CRLF warnings; Playwright at `http://127.0.0.1:8080/` confirmed `已阅 -> 已阅栏目 -> 恢复`, no console errors, and no read-state localStorage residue after recovery. User confirmed acceptance.
- Collection-window verification on 2026-07-05: `.\.venv\Scripts\python.exe -m unittest tests.test_local_server -q` passed with 60 tests; `.\.venv\Scripts\python.exe -m unittest discover -s tests -q` passed with 209 tests; `node --check assets/app.js` and `.\.venv\Scripts\python.exe -m py_compile scripts\local_server.py` passed. Local HTTP `http://127.0.0.1:8080/index.html` returned 200 and served `自上次采集`. A direct `refresh_command(root, "24h")` probe read last collection time `2026-07-05 09:06:36.561958+00:00` and produced `--collect-window-hours 7`; `refresh_command(root, "all")` omitted that argument. After restarting the local server and rerunning real collection, `data/source-status.json` moved from `2026-07-05T15:21:58.901276Z` to `2026-07-05T15:33:40.837860Z` and wrote `collection_window_hours=1`, matching ceil of the elapsed 11m42s.

## 维护规则

- `PROJECT_STATE.md` 只记录当前状态与最近一周的关键交接。
- 旧状态条目移入 `docs/PROJECT_STATE_ARCHIVE.md`。
- 刷新、采集等操作手册放入 `docs/guides/refresh-playbooks.md`。
